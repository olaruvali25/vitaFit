/**
 * Normalize Emails Script - Supabase version
 * 
 * This script is now a no-op since Supabase handles email normalization.
 */

console.log("⚠️ This script is deprecated.")
console.log("Supabase Auth handles email normalization automatically.")
console.log("No action needed.")

process.exit(0)
